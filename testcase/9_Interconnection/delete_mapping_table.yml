-  name: 删除映射表
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/connection/2/${get_extract_data(copy_maptable_id)}
      method: delete
      headers:
         X-Project: ${get_extract_data(x_project)}
   validate:
      -  equals: {status_code: 200}