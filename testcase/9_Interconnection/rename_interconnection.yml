-  name: 重命名互联项
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/connection/rename/1/${get_extract_data(intn_id)}/重命名_mysql${get_random_number(1,999)}
      method: put
      headers:
         X-Project: ${get_extract_data(x_project)}
   validate:
      -  equals: {status_code: 200}