-  name: 开启互联项
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/connection/switch/1/${get_extract_data(intn_id)}/on
      method: put
      headers:
         X-Project: ${get_extract_data(x_project)}
   validate:
      -  equals: {status_code: 200}