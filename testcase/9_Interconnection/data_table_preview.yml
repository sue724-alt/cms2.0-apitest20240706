-  name: 预览数据表
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/connection/${get_extract_data(intn_id)}/preview
      method: post
      json:
        {
           "isBasic": true,
           "databaseTableName": "student",
           "querySql": "",
           "limited": 10
        }
      headers:
         X-Project: ${get_extract_data(x_project)}
   validate:
      -  equals: {status_code: 200}