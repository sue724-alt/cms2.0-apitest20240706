-  name: 获取所有语言列表类型
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/multiplelanguage/languagelist/languagetype
      method: get
      headers:
         X-Project: ${get_extract_data(x_project)}
   validate:
      -  equals: {status_code: 200}