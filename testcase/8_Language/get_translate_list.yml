-  name: 获取翻译文本列表
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/multiplelanguage/19/0/2/
      method: get
      headers:
         X-Project: ${get_extract_data(x_project)}
   extract:
      translate_id: originalId.0
   validate:
      -  equals: {status_code: 200}