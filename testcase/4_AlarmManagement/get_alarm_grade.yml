-  name: 查询报警等级
   base_url: ${get_base_url(base_url)}
   request:
      url: /api/v1/alarmlevel/query
      method: get
      headers:
         Content-Type: application/json;charset=UTF-8
         X-Project: ${get_extract_data(x_project)}
   validate:
   - equals:
       status_code: 200