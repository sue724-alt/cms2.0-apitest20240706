-  name: 获取通道信息
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/variable/tunnel/io/protocol
      method: get
      headers:
         X-Project: ${get_extract_data(x_project)}
   validate:
      -  equals: {status_code: 200}
      -  contains: tree