-  name: $csv{name}排序到$csv{sort}
   base_url: ${get_base_url(cms_url)}
   parameters:
      name-TableId-nodeType-sort: data/DataManagement/set_sorting.csv
   request:
      url: /api/v1/dataconfig/node/sort/$csv{nodeType}/${get_extract_data($csv{TableId})}/$csv{sort}
      method: put
      headers:
         Content-Type: application/json;charset=UTF-8
         X-Project: ${get_extract_data(x_project)}
   validate:
   - equals:
       status_code: 200