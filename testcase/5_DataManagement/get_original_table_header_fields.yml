-  name: 获取归档表表头字段设置
   base_url: ${get_base_url(cms_url)}
   request:
      url: /api/v1/dataconfig/filingtable/${get_extract_data(Original_Table_ID)}/headers
      method: get
      headers:
         Content-Type: application/json; =UTF-8
         X-Project: ${get_extract_data(x_project)}
   validate:
   -  equals: {status_code: 200}